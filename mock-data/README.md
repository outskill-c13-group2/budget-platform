# Mock data implementations

Each implementation is deterministic demo data, not production data. It must validate against `../contracts/fixture.schema.json` and use integer minor units for all money.

Keep one fixture per scenario. The first fixture implements the agreed September clothing rebalance: +$200 for Teen 1, funded by -$100 from each parent, so the change nets to zero.
